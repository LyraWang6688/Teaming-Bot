# 服务器运行保护与 Base 恢复验收

验收日期：2026-09-20（北京时间）。本次为用户授权的服务器测试与两条指定 Base 任务恢复，不代表此前 F01–F08 全部故障场景已验收。

## 部署状态

- 服务器：43.143.239.42，目录 `/home/ubuntu/meeting-analysis`。
- 基础 Git 版本：`14908c75ba54fe68b568477d7faedfc2b58ec0f2`。
- 本轮补丁 SHA256：`1fd61925eae8565a3302f2eacebd0a5242b62a41bc23a156ebc2cc6fb88f79b8`。
- 已部署镜像标识：`14908c7+runtime-1fd61925eae8`，写入镜像 label 和 `APP_REVISION`；可区别 Git 目录版本与实际运行镜像。
- 新容器启动时间：2026-09-20 09:29:03，北京时间。
- 旧镜像保留为 `meeting-analysis:before-server-only-20260919`。回退该镜像将失去本轮运行保护，不能无条件回退并重发历史任务。
- 2026-09-20 验收时，保护代码已同步本地和服务器工作区并部署，尚未提交 GitHub。后续 GitHub 同步以版本库提交记录为准；上述镜像标识记录的是本次实际部署，服务器工作区中的改动应在后续更新前核对并保留。

## 防护与验证

- `pnpm dev` 直接返回禁止启动提示；未启动本地开发服务。
- 监听、分析执行器、交付启动和通知发送要求生产环境且 `FEISHU_RUNTIME_ENABLED=true`。
- 服务器 Compose 显式开启后台；构建镜像期间未开启后台。
- 报告域名不再默认 localhost，要求公网 HTTPS 域名；持久化通知地址在发送前再次校验，非法任务 blocked。
- 三个隔离单元测试通过，覆盖开发环境禁止、生产显式启用、非法地址拒绝；TypeScript、ESLint、服务器生产构建通过。
- 公网配置页面返回 HTTP 200。新容器日志确认交付 worker 和事件 listener 已启动。

## 恢复结果（真实飞书回读）

| 会议 | Base 记录 ID | 状态 | 同步版本 |
|---|---|---|---|
| ABC业务解耦测试 | recvvIH4XrTHSv | succeeded，partial=false | 5/5 |
| ABC 会议分析讨论 | recvvIH33QvX8l | succeeded，partial=false | 2/2 |

恢复前检查了原项目、组织方向和目标表匹配，限定任务 ID、blocked、无租约、未同步、无 Base 记录的条件。原缺凭据任务重新入队；另一条缺快照任务从其原项目补全加密目标配置。恢复动作已写入 `feishu_audit_logs`，action 为 `meeting.delivery.base_sync.recovered`。

通过服务器现有 Base 应用凭据调用飞书 `records/batch_get`，核对两条记录的会议 ID、会议名称、分类、完整文字稿、报告链接，十项比对均为 true。未输出任何凭据或文字稿内容。

错误报告 URL 已在 `meeting_records` 修正为：
`https://meeting.bamamei.online/report/v2/2a73727c-261e-4be3-81e3-6c5cd061fa36`

报告 URL 修复也有审计记录。分析完成时间仍是 9 月 19 日原时间，通知 message_id/sent_at 均未变化，没有重新分析或补发消息。已发送卡片中的旧按钮不会随数据库修复自动改变；保留通知任务原地址作为发送历史，需用户另行授权更新/补发卡片。

## 边界

本次验证了已有完整报告的 Base 恢复及真实内容一致性，没有新建会议，也没有进行模型调用、故障注入或重发通知。新会议全流程及其他并发/崩溃验收应单独安排。
