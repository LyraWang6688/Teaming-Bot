# 飞书集成设计文档

> 最后更新时间：2026-06-24

## 一、概述

当前飞书集成正在从 `Webhook + OpenAPI + 服务器环境变量` 迁移到 `Webhook + OpenAPI + Supabase(Auth/Postgres) + 用户级集成配置`。

目标闭环：

- 接收会议结束事件
- 将会议基础信息写入多维表格
- 获取会议录制文件并解析妙记 token
- 导出妙记文字稿
- 调用豆包分析
- 回写多维表格
- 打开报告页

## 二、技术架构

### 2.1 当前正式方案

当前生产链路统一使用 Webhook 接收 `vc.meeting.participant_meeting_ended_v1`，再由服务端通过 OpenAPI 获取会议录制文件、解析 `minute_token`、导出文字稿，并写入多维表格。其中 Webhook、Base 与恢复任务继续使用 `tenant_access_token`，录制文件与妙记文字稿在单 owner 模式下优先使用 `user_access_token`，失败时再回退到 `tenant_access_token`。

```text
飞书事件
  ↓
/api/feishu/webhook
  ↓
webhookProcessor.ts
  ↓
recording / transcript / analysisService.ts / bitableOpenApi.ts
  ↓
多维表格 + 报告页
```

### 2.2 为什么使用会议结束事件

当前自动化链路以“用户参与的会议已经结束”为起点。事件体中直接提供 `meeting.id`、`topic`、`start_time`、`end_time`、`owner.id.open_id`、`host_user.id.open_id`，足以完成建档与后续录制文件轮询准备。

### 2.3 新的多租户目标架构

平台级配置继续保留在全局 `.env` 中：

- `PROJECT_PUBLIC_URL`
- 大模型配置
- `DATABASE_URL`
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `APP_ENCRYPTION_KEY`

用户级飞书配置迁移到数据库：

- 飞书应用配置：`App ID / App Secret / Webhook Verification Token`
- Base 配置：`Base App Token / Table ID / 初始化状态`
- OAuth 授权：`access_token / refresh_token / expires_at`
- 检查状态：权限、事件订阅、Webhook、OAuth、Base
- 审计日志与会议主索引

## 三、订阅事件清单

| 事件名称 | 事件 ID | 用途 |
|---------|--------|------|
| 参与会议结束 | `vc.meeting.participant_meeting_ended_v1` | 作为整条自动化链路的唯一事件入口 |

## 四、所需权限清单

### 4.1 多维表格权限

| 权限 ID | 用途 |
|--------|------|
| `bitable:app:read` | 读取多维表格信息 |
| `bitable:table:read` | 读取数据表结构 |
| `bitable:record:read` | 查询和批量获取记录 |
| `bitable:record:write` | 新增和更新记录 |

### 4.2 会议与妙记权限

| 权限 ID | 用途 |
|--------|------|
| `vc:meeting.meetingevent:read` | 订阅 `vc.meeting.participant_meeting_ended_v1` |
| `vc:record:readonly` | 获取会议录制文件 |
| `minutes:minutes.transcript:export` | 导出妙记转写文字内容 |

## 五、前端配置页职责

页面位置：`/feishu-config`

配置页目标升级为 5 步初始化向导：

1. 创建飞书应用并填写基础配置
2. 配置权限、事件订阅和回调地址
3. 初始化多维表格
4. 完成 OAuth 授权
5. 执行真实 API 检查并完成初始化

当前已落地的前端行为：

- 未登录用户访问 `/feishu-config` 时，只显示说明与登录入口；保存动作要求先完成 `Supabase Auth` 登录
- 登录后页面会调用 `/api/auth/me` 识别当前用户，再读取 `/api/feishu/integrations` 获取该用户自己的集成列表
- 页面支持创建新集成、选择已有集成、编辑基础配置，并通过 `POST / PATCH /api/feishu/integrations` 真实写入数据库
- 已保存的 `App Secret`、`Webhook Verification Token`、`Base App Token` 仅以脱敏形式回显，前端不再接收密钥明文
- 选中某个集成后，页面通过 `/api/feishu/integrations/[integrationId]` 读取该集成的 OAuth 摘要、检查状态、必需权限与必需事件
- 点击“授权当前集成”时，页面使用 `/api/feishu/oauth/start?integrationId=...` 发起与当前集成绑定的 OAuth
- 点击“一键初始化 Base”时，页面会调用 `/api/feishu/integrations/[integrationId]/base/initialize`，自动创建 Base、复用或补齐会议信息表字段，并把 `Base App Token / Meeting Table ID / initializedAt` 回写到当前集成
- 点击“执行真实检查”时，页面会调用 `/api/feishu/integrations/[integrationId]/checks`，在线校验应用凭证、OAuth 用户身份、Webhook 命中状态与 Base 资源访问情况，并把结果写回 `feishu_integration_checks`

当前阶段的页面边界：

- “真实检查” 当前已支持应用凭证、OAuth、Webhook/Base 联通的在线校验；`权限` 状态会基于 `tenant credentials + oauth user + bitable access` 综合推断，会议录制与妙记导出权限仍会在首条真实会议链路中继续验证
- `事件订阅` 当前通过“是否已经收到 Webhook challenge / 真实事件回调”间接判定，尚未直接读取飞书开放平台后台的订阅配置
- 页面默认聚焦“每个用户维护自己的 1 套主集成”，但数据结构与列表 UI 已兼容一个用户拥有多套集成

用户负责：

- 在飞书开放平台创建企业自建应用
- 开权限、配事件、配置 Webhook/OAuth 回调
- 点击 OAuth 授权

系统负责：

- 存储用户级飞书集成配置
- 使用内部状态模型做真实 API 检查
- 自动创建 Base 和字段
- 自动保存 OAuth token 并续期
- 记录初始化状态与审计日志
- 运行会议分析自动化链路

## 六、多维表格结构

当前运行时依赖一张会议信息表，字段如下：

| 字段名 | 字段类型 | 用途 |
|-------|---------|------|
| 会议ID | 文本 | 飞书会议唯一标识 |
| 会议主题 | 文本 | 会议标题 |
| 开始时间 | 日期时间 | 会议开始时间 |
| 结束时间 | 日期时间 | 会议结束时间 |
| 组织者 | 文本 | 会议拥有者或主持人的 open_id |
| 处理状态 | 单选 | 会议结束 / 获取录制文件中 / 获取文字稿中 / 分析中 / 已完成 / 失败 |
| 会议文字稿 | 文本 | 妙记转写全文 |
| 分析摘要 | 文本 | LLM 分析摘要 |
| 报告链接 | URL | `/report?recordId=xxx` 报告页面链接；写入时需使用 `{ text, link }` 结构 |
| JSON数据 | 文本 | 完整 AnalysisResult JSON |
| 错误信息 | 文本 | 异常原因，用于排障 |

## 七、完整流程

### 7.1 初始化流程

1. 用户登录平台（`Supabase Auth`）。
2. 在 `/feishu-config` 中填写 `App ID / App Secret / Webhook Verification Token`。
3. 用户在飞书开放平台中补齐权限、事件订阅和回调地址：
   - Webhook：`/api/feishu/webhook`
   - OAuth 回调：`/api/feishu/oauth/callback`
4. 用户点击“授权飞书”，系统通过 `state -> user_id + integration_id` 绑定回调结果，并将 OAuth token 直接写入数据库。
5. 用户点击“一键初始化 Base”后，系统会自动创建 Base App（或复用现有 Base）、定位/创建会议信息表，并补齐运行时依赖字段。
6. 系统把 `Base App Token / Table ID / initializedAt` 写回该用户的集成配置。
7. 用户点击“执行真实检查”后，系统在线验证应用凭证、OAuth、Webhook 和 Base 联通情况，并更新权限、事件订阅、Webhook、OAuth、Base 状态。
8. 当所有状态通过后，该用户进入“可自动分析”状态。

### 7.2 会议分析流程

1. 飞书发送 `vc.meeting.participant_meeting_ended_v1`。
2. `/api/feishu/webhook` 验签、去重、入队。
3. 服务端从事件体中直接提取 `meeting.id`、`topic`、`start_time`、`end_time`、`owner.id.open_id`；若 `owner.id.open_id` 缺失，则回退到 `host_user.id.open_id`，再回退到 `operator.id.open_id`。
4. 服务端先把 `会议ID`、`会议主题`、`开始时间`、`结束时间`、`组织者` 写入多维表格，并将状态写为 `会议结束`。
5. 首次建档时以 `meeting.id` 作为幂等键防止重复创建；一旦 Base 返回 `recordId`，后续状态更新、恢复链路和继续处理都优先使用 `recordId` 命中同一条记录。
6. 服务端在会议结束后第 `2`、`3`、`4`、`5` 分钟各执行一次轮询尝试，先调用 `GET /open-apis/vc/v1/meetings/{meeting_id}/recording` 获取录制文件，状态变更为 `获取录制文件中`。若已配置 `FEISHU_USER_*`，该接口会优先使用 `user_access_token`，失败后再回退到 `tenant_access_token`。
7. 若接口返回 `recording.url`，服务端从 URL 最后一段解析出 `minute_token`；若录制仍在生成中，则继续等待下一轮窗口。
8. 服务端调用 `minutes/v1/minutes/:minute_token/transcript?need_speaker=true&need_timestamp=true&file_format=txt` 导出文字稿，状态变更为 `获取文字稿中`；若已配置 `FEISHU_USER_*`，该接口同样会优先使用 `user_access_token`，失败后再回退到 `tenant_access_token`；若妙记转写尚未完成，则继续等待下一轮窗口。
9. 调用豆包 API 分析，状态变更为 `分析中`；若豆包返回临时性错误，会在服务内做最多 3 次短间隔重试。
10. 回写 `会议文字稿`、`分析摘要`、`报告链接`、`JSON数据`，状态变更为 `已完成`。其中 `报告链接` 若为多维表格超链接字段，必须按 `{ text: 会议主题, link: 报告URL }` 结构写入，不能直接传普通字符串，否则可能触发 `URLFieldConvFail`。
11. 用户通过 `/report?recordId=...` 打开报告页。

### 7.3 可靠性机制

1. `获取录制文件` 与 `导出妙记文字记录` 在单 owner 模式下优先使用 `user_access_token`；若未配置用户 token 或 user 身份调用失败，则自动回退到 `tenant_access_token`。
2. 若第 2/3/4/5 分钟窗口内录制文件仍未生成，系统不会提前写成 `失败`，而是继续等待下一次窗口；若 5 分钟窗口结束仍无结果，则写入业务可理解的失败原因。
3. 若导出文字稿时飞书返回 `minute not ready` 或临时性 5xx/429 错误，系统会继续等待下一次窗口；若所有窗口耗尽仍未就绪，则写回失败状态。
4. 单进程内对同一会议持有处理锁，避免同一会议被并发导出、并发分析；锁超时后允许后续事件重新进入链路，防止记录长期卡住。
5. Base 回读时会统一把文本单元格和富文本片段数组归一化为纯字符串，避免启动恢复时因字段返回格式差异读不出 `meetingId`、`会议主题` 或 `组织者`。
6. Webhook 处理链路输出结构化监控日志，统一记录 `meetingId`、`recordId`、`attempt`、`organizerSource`、`minuteToken`、错误类型和重试行为，便于在应用日志中追踪录制文件、文字稿、分析和恢复过程。
7. 服务启动时会扫描 Base 中状态为 `会议结束 / 获取录制文件中 / 获取文字稿中 / 分析中` 的记录，并自动恢复未完成链路：
   - 若记录处于 `分析中` 且已写入 `会议文字稿`，则直接从文字稿继续分析并回写结果；
   - 其他活跃状态会保留已知 `recordId` 直接继续“获取录制文件 / 导出文字稿 / 分析”主流程。
8. 启动恢复默认开启，可通过环境变量 `FEISHU_ENABLE_STARTUP_RECOVERY=false` 显式关闭；该恢复机制依赖当前 Base 中已有的 `处理状态`、`会议文字稿`、`错误信息` 等字段，不要求新增多维表格字段。

## 八、部署与调试

### 8.1 Webhook 地址

```text
https://meeting.bamamei.online/api/feishu/webhook
```

### 8.2 部署结构

```text
Nginx 443
  ↓
127.0.0.1:3011
  ↓
Docker 容器内 Next.js :3000
```

### 8.3 生产环境变量

```env
PROJECT_PUBLIC_URL=https://meeting.bamamei.online
DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
APP_ENCRYPTION_KEY=

ANALYSIS_PROVIDER=doubao
DOUBAO_API_KEY=
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=doubao-seed-1-8-251228

# 过渡阶段兼容：尚未迁移到数据库前，单实例仍可使用以下飞书 env
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_WEBHOOK_VERIFICATION_TOKEN=
FEISHU_BASE_APP_TOKEN=
FEISHU_MEETING_TABLE_ID=
FEISHU_ENABLE_STARTUP_RECOVERY=true
```

## 九、注意事项

- `tenant_access_token` 在服务内存中缓存，并在过期前自动刷新。
- 每个飞书事件都有唯一 `event_id`，需要去重避免重复分析。
- 当前正式链路仍以应用身份承接 Webhook、Base 与恢复任务；若要打通单 owner 模式下的录制与妙记权限链路，建议配置 `FEISHU_USER_*` 环境变量。
- OAuth 默认 scope 已包含 `offline_access`，用于换取 `refresh_token` 并自动刷新 `user_access_token`。
- 多租户阶段下，所有用户级敏感字段都必须加密后写库；前端仅展示脱敏值，日志不得打印完整 token 或 secret。
- Webhook 路由入口现已优先基于 `Webhook Verification Token -> integration` 动态识别集成，并兼容旧的全局 `FEISHU_WEBHOOK_VERIFICATION_TOKEN` 回退；OAuth 回调绑定统一基于 `state -> user_id + integration_id`。
- 当 webhook 命中数据库中的某个集成后，系统会回写该集成的 `lastWebhookReceivedAt`，并把 `webhookStatus / eventSubscriptionStatus` 更新为 `success`，便于在 `/feishu-config` 中直接观察回调是否打通。
- Base 初始化当前会自动补齐以下字段：`会议ID`、`会议主题`、`开始时间`、`结束时间`、`组织者`、`处理状态`、`会议文字稿`、`分析摘要`、`报告链接`、`JSON数据`、`错误信息`。
- 平台数据库需要保留会议主索引；飞书 Base 负责给用户展示结果和承接写回。
- 当前实现通过 `meeting_id -> recording.url -> minute_token -> transcript` 获取文字稿，不再依赖标题、组织者和时间窗搜索妙记。
- 若未开通 `vc:record:readonly` 或 `minutes:minutes.transcript:export`，系统会将错误写回 `错误信息` 字段，提示检查应用权限或资源范围。
- 若重试耗尽或分析最终失败，需要将失败状态和 `错误信息` 写回多维表格，方便排障。
- 结构化监控日志目前直接输出到应用日志，建议部署时重点关注以下事件：`recording_fetch_finished`、`transcript_export_finished`、`analysis_failed`、`meeting_pipeline_failed`、`startup_recovery_scan_finished`。
- 启动恢复面向“单机 + 单用户”第一阶段，主要解决服务重启后内存定时器丢失的问题；事件去重集合和轮询重试计划仍然是进程内状态，后续若进入稳定生产阶段，建议迁移到队列或数据库。
