# 项目结构说明

> 最后更新时间：随代码变更同步更新

## 目录结构总览

```text
/workspace/projects/
├── .git/                          # Git 版本控制
├── .gitignore                     # Git 忽略规则
├── docs/                          # 项目文档
├── public/                        # 静态资源目录
├── src/                           # 源代码目录
├── AGENTS.md                      # 项目开发规范
├── Dockerfile                     # 生产容器镜像构建配置
├── docker-compose.yml             # 私服部署编排（127.0.0.1:3011 -> 容器 3000）
├── .env.production.example        # 生产环境变量样例
├── drizzle.config.ts              # Drizzle / Supabase Postgres 配置
├── package.json                   # 依赖管理
├── tsconfig.json                  # TypeScript 配置
├── next.config.ts                 # Next.js 配置
├── eslint.config.mjs              # ESLint 配置
└── README.md                      # 项目说明文档
```

## 根目录配置文件

| 文件 | 用途 | 说明 |
|------|------|------|
| `package.json` | 依赖管理 | 定义项目依赖和脚本 |
| `pnpm-lock.yaml` | 依赖锁定 | 锁定依赖版本 |
| `tsconfig.json` | TypeScript 配置 | TypeScript 编译器配置 |
| `next.config.ts` | Next.js 配置 | Next.js 运行配置 |
| `eslint.config.mjs` | ESLint 配置 | 代码规范检查配置 |
| `Dockerfile` | 容器构建 | 固化 Node/pnpm/Next.js 运行环境，便于迁移 |
| `docker-compose.yml` | 容器部署 | 将宿主机 `127.0.0.1:3011` 映射到容器 `3000` |
| `.env.production.example` | 环境变量样例 | 平台级配置、豆包 API、公网域名与任务恢复开关 |
| `drizzle.config.ts` | 数据库配置 | Drizzle 针对 Supabase Postgres 的 schema/generate/push 配置 |

## src/ 目录结构

```text
src/
├── app/                           # Next.js App Router
│   ├── api/                       # API 路由
│   │   ├── analyze/               # 会议分析 API
│   │   │   ├── route.ts           # 同步分析兼容接口
│   │   │   └── tasks/             # 网页上传异步分析任务创建与轮询接口
│   │   └── feishu/                # 飞书集成 API
│   │       ├── integrations/      # 用户级飞书集成配置 CRUD
│   │       ├── integrations/[integrationId]/authorize/ # CLI 用户授权入口与轮询
│   │       ├── integrations/[integrationId]/event-subscription/ # CLI 事件监听校验
│   │       ├── integrations/[integrationId]/base/initialize/ # Base 初始化接口
│   │       └── record/            # 报告记录读取 API
│   ├── feishu-config/             # 飞书接入四步向导页
│   ├── report/page.tsx            # 飞书报告页（/report?recordId=xxx）
│   └── page.tsx                   # 首页
├── instrumentation.ts             # 启动恢复与运行时初始化
├── components/                    # React 组件
│   ├── FeishuConfigWorkspace.tsx  # 飞书配置四步向导主体与左侧纵向步骤栏
├── constants/                     # 常量配置
├── hooks/                         # 自定义 Hooks
├── lib/                           # 工具库
│   ├── feishu/                    # 飞书集成工具
│   │   ├── bitableOpenApi.ts      # 集成级多维表格读写
│   │   ├── events/                # lark-cli event consume 事件监听管理
│   │   ├── integrationOpenApi.ts  # 基于 CLI profile 的用户身份 OpenAPI 调用封装
│   │   │   ├── meetingPipelineTaskStore.ts # 会议处理任务落库、恢复与调度状态存储
│   │   │   ├── meetingPipelineWorker.ts # 轮询领取数据库任务并执行会议处理
│   │   ├── integrationSetup.ts    # 飞书真实检查与 Base 初始化编排
│   │   ├── integrationStore.ts    # 用户级飞书集成 / CLI 授权 / 检查状态 / 审计日志存储
│   │   ├── monitor.ts             # 飞书链路结构化监控日志
│   │   ├── openapi.ts             # 飞书通用错误类型定义
│   │   ├── status.ts              # 飞书六段处理状态机
│   │   ├── transcript.ts          # 逐字稿文档读取
│   │   ├── types.ts               # 飞书相关类型定义
│   │   └── pipeline/              # 妙记生成事件后的异步处理管线
│   ├── db/                        # 数据库 schema 与连接
│   │   ├── client.ts              # Drizzle + Postgres 连接
│   │   └── schema.ts              # 多租户飞书集成、授权、检查、审计、会议主索引、飞书任务表与网页分析任务表
│   ├── analysis/                  # 网页上传解析、任务存储与后台执行
│   ├── platform/                  # 平台级环境变量读取
│   │   └── env.ts                 # Supabase / Database / Encryption 等全局配置
│   ├── security/                  # 安全与敏感信息处理
│   │   └── crypto.ts              # 统一 encrypt / decrypt / hash / mask 实现
│   ├── supabase/                  # Supabase 客户端封装
│   │   ├── browser.ts             # 浏览器端客户端
│   │   ├── server.ts              # 服务端认证客户端
│   │   └── service.ts             # Service Role 客户端
│   └── utils.ts                   # 通用工具函数
├── services/                      # 服务层
│   ├── analysisService.ts         # 统一会议分析入口
│   └── llm/                       # 豆包 Provider 抽象
├── types/                         # TypeScript 类型定义
└── utils/                         # 工具函数
```

## 核心链路

1. 手动入口：用户上传会议纪要，调用 `src/app/api/analyze/tasks/route.ts` 创建 `web_analysis_tasks` 异步任务，前端轮询 `src/app/api/analyze/tasks/[taskId]/route.ts` 获取结果。
2. 配置入口：`src/app/feishu-config/page.tsx` + `src/components/FeishuConfigWorkspace.tsx` 展示飞书接入四步向导和初始化目标状态。
3. 用户级配置入口：`src/app/api/feishu/integrations/*` 负责按登录用户读写飞书集成配置。
4. 应用创建入口：`src/app/api/feishu/integrations/create-app/route.ts` 通过 `lark-cli config init --new` 创建用户独立应用。
5. 用户授权入口：`src/app/api/feishu/integrations/[integrationId]/authorize/*` 通过 CLI Device Flow 完成用户权限授权。
6. 事件校验入口：`src/app/api/feishu/integrations/[integrationId]/event-subscription/check/route.ts` 启动 `minutes.minute.generated_v1` 监听并等待 ready marker。
7. Base 初始化入口：`src/app/api/feishu/integrations/[integrationId]/base/initialize/route.ts` 自动创建 Base，并新建只包含业务字段的“会议信息”数据表。
8. `analysisService.ts` 统一调用豆包 API，返回结构化分析结果。
9. `webAnalysisTaskStore.ts` / `webAnalysisTaskProcessor.ts` 负责网页上传任务的落库、领取、执行、完成/失败状态更新。
10. `eventListenerManager.ts` 负责消费 CLI 输出的妙记生成事件，并把事件写入数据库任务表。
11. `meetingPipelineTaskStore.ts` 负责承接会议任务的落库、领取、重试计划、完成/失败状态与启动恢复读取。
12. `meetingPipelineWorker.ts` 作为常驻任务执行器，轮询数据库领取到期任务，再执行 `minute_token -> transcript -> analysis -> Base 回写` 主链路。
13. `src/instrumentation.ts` 在服务启动时先拉起任务执行器，再基于数据库任务表执行启动恢复。
14. `src/lib/feishu/monitor.ts` 统一输出飞书录制文件、导出、分析和恢复的结构化日志。
15. 报告页 `src/app/report/page.tsx` 通过 `recordId` 从 Base 读取 `JSON数据` 渲染报告。

## 依赖说明

| 包 | 用途 |
|-----|------|
| `next` | Next.js 框架 |
| `react` | React 库 |
| `typescript` | TypeScript 编译器 |
| `mammoth` | Word 文档（.docx）解析 |
| `html-to-image` | DOM 转图片，用于 PDF 导出 |
| `jspdf` | PDF 生成 |
| `recharts` | 图表绘制 |
| `drizzle-orm` / `drizzle-kit` | 数据模型、SQL 生成与推送 |
| `pg` | PostgreSQL / Supabase 数据库连接 |
| `@supabase/supabase-js` / `@supabase/ssr` | Supabase Auth 与服务端会话管理 |

## 相关文档

- [后台分析逻辑说明.md](./后台分析逻辑说明.md)
- [输出报告的内容与形式说明.md](./输出报告的内容与形式说明.md)
- [飞书集成设计.md](./飞书集成设计.md)
- [系统架构图.md](./系统架构图.md)
- [AGENTS.md](../AGENTS.md)
