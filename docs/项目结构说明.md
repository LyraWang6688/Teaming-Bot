# 项目结构说明

> 最后更新时间：2026-07-22

## 目录结构总览

```text
/workspace/projects/
├── .git/                          # Git 版本控制
├── .gitignore                     # Git 忽略规则
├── docs/                          # 项目文档
├── public/                        # 静态资源目录
├── src/                           # 源代码目录
├── supabase/migrations/           # Supabase Postgres 版本化迁移 SQL
├── AGENTS.md                      # 项目开发规范
├── Dockerfile                     # 生产容器镜像构建配置
├── docker-compose.yml             # 私服部署编排（127.0.0.1:3011 -> 容器 3000）
├── .env.production.example        # 生产环境变量样例
├── drizzle.config.ts              # Drizzle / Supabase Postgres 配置
├── package.json                   # 依赖管理
├── tsconfig.json                  # TypeScript 配置
├── next.config.ts                 # Next.js 配置
├── eslint.config.mjs              # ESLint 配置
└── README.md                      # 项目说明文档
```

## 根目录配置文件

| 文件 | 用途 | 说明 |
|------|------|------|
| `package.json` | 依赖管理 | 定义项目依赖和脚本；生产构建固定使用 Webpack 兼容飞书 SDK 与现有 Babel 配置 |
| `pnpm-lock.yaml` | 依赖锁定 | 锁定依赖版本 |
| `tsconfig.json` | TypeScript 配置 | TypeScript 编译器配置 |
| `next.config.ts` | Next.js 配置 | Next.js 运行配置 |
| `eslint.config.mjs` | ESLint 配置 | 代码规范检查配置 |
| `Dockerfile` | 容器构建 | 固化 Node/pnpm/Next.js 运行环境，便于迁移 |
| `docker-compose.yml` | 容器部署 | 将宿主机 `127.0.0.1:3011` 映射到容器 `3000` |
| `.env.production.example` | 环境变量样例 | 平台级配置、豆包 API、公网域名与任务恢复开关 |
| `drizzle.config.ts` | 数据库配置 | Drizzle 针对 Supabase Postgres 的 schema/generate/push 配置 |
| `supabase/migrations/` | 数据库迁移 | 保存可审计、可重复部署的 Supabase Postgres 结构变更 SQL，包括移除不再使用的组织字段模板缓存状态 |

## src/ 目录结构

```text
src/
├── app/                           # Next.js App Router
│   ├── api/                       # API 路由
│   │   ├── analyze/               # 会议分析 API
│   │   │   ├── route.ts           # 同步分析兼容接口
│   │   │   └── tasks/             # 网页上传异步分析任务创建与轮询接口
│   │   └── feishu/                # 飞书集成 API
│   │       ├── integrations/      # 用户级飞书集成配置 CRUD
│   │       ├── integrations/[integrationId]/authorize/ # 标准 OAuth 授权入口与状态轮询
│   │       ├── integrations/[integrationId]/event-subscription/ # SDK 长连接启动与校验
│   │       ├── oauth/callback/    # 飞书 OAuth 回调、Token 加密落库与页面重定向
│   │       └── record/            # 按组织目标 Base 读取报告记录 API
│   ├── feishu-config/             # 飞书接入五步向导页
│   ├── report/page.tsx            # 飞书报告页（/report?recordId=xxx）
│   └── page.tsx                   # 首页
├── instrumentation.ts             # 启动恢复与运行时初始化
├── components/                    # React 组件
│   ├── AnalysisDashboard.tsx      # 新版四部分会议分析报告模板
│   ├── FeishuConfigWorkspace.tsx  # 飞书配置五步状态机、局部刷新与完成烟花
│   └── charts/                    # 报告图表组件
│       ├── NetworkGraph.tsx       # 团队互动网络图（角色 + 发言占比 + 互动强度）
│       └── TeamStateChart.tsx     # Zone 四象限图
├── constants/                     # 常量配置
├── hooks/                         # 自定义 Hooks
├── lib/                           # 工具库
│   ├── feishu/                    # 飞书集成工具
│   │   ├── bitable/               # 组织目标多维表格读取与写入
│   │   ├── events/                # SDK WSClient 长连接与事件消费管理
│   │   ├── integration/           # 飞书集成配置、仓储与 SDK 服务
│   │   │   ├── appRegistrationStore.ts   # registerApp 短期交互态（非凭证事实来源）
│   │   │   ├── appRegistrationService.ts # 创建结果加密落库与应用配置编排
│   │   │   ├── applicationConfigService.ts # OAuth 回调、Refresh Token、长连接与发布
│   │   │   ├── integrationConstants.ts   # 用户权限、应用自管理权限与事件清单
│   │   │   ├── integrationOpenApi.ts     # SDK 用户身份 OpenAPI 请求封装
│   │   │   ├── integrationSetup.ts       # 六段真实校验、妙记订阅与监听启动编排
│   │   │   ├── integrationStore.ts       # 集成、授权、检查、OAuth state 与审计仓储
│   │   │   ├── sdkClient.ts              # 按 integration 创建 SDK Client
│   │   │   └── tokenService.ts           # 数据库 Token 读取、刷新与失效检测
│   │   ├── common/                # 飞书通用错误、监控、状态和类型
│   │   ├── minutes/               # 妙记文字稿导出
│   │   ├── pipeline/              # 妙记生成事件后的异步处理管线
│   │   │   ├── meetingPipelineProcessor.ts # 事件解析、幂等与任务入队
│   │   │   ├── meetingPipelineTaskStore.ts # 任务落库、恢复与调度状态存储
│   │   │   └── meetingPipelineWorker.ts    # 领取任务并执行会议处理
│   │   └── projects/              # 服务器项目与组织目标配置仓储
│   ├── db/                        # 数据库 schema 与连接
│   │   ├── client.ts              # Drizzle + Postgres 连接
│   │   └── schema.ts              # 多租户飞书集成、授权、检查、审计、会议主索引、飞书任务表与网页分析任务表
│   ├── analysis/                  # 网页上传解析、任务存储与后台执行
│   ├── platform/                  # 平台级环境变量读取
│   │   └── env.ts                 # Supabase / Database / Encryption 等全局配置
│   ├── security/                  # 安全与敏感信息处理
│   │   └── crypto.ts              # 统一 encrypt / decrypt / hash / mask 实现
│   ├── supabase/                  # Supabase 客户端封装
│   │   ├── browser.ts             # 浏览器端客户端
│   │   ├── server.ts              # 服务端认证客户端
│   │   └── service.ts             # Service Role 客户端
│   └── utils.ts                   # 通用工具函数
├── services/                      # 服务层
│   ├── analysisService.ts         # 统一会议分析入口（Project 2 对齐版内核）
│   └── llm/                       # 豆包 Provider 抽象
├── types/                         # TypeScript 类型定义（含 dialogueNetwork / unfinishedDialogues 等新版结构）
└── utils/                         # 工具函数
```

## 核心链路

1. 手动入口：用户上传会议纪要，调用 `src/app/api/analyze/tasks/route.ts` 创建 `web_analysis_tasks` 异步任务，前端轮询 `src/app/api/analyze/tasks/[taskId]/route.ts` 获取结果。
2. 配置入口：`src/app/feishu-config/page.tsx` + `src/components/FeishuConfigWorkspace.tsx` 展示飞书接入五步向导；交互只更新局部状态，不执行整页刷新。
3. 用户级配置入口：`src/app/api/feishu/integrations/*` 负责按登录用户读写飞书集成配置。
4. 应用创建入口：`src/app/api/feishu/integrations/create-app/route.ts` 通过 SDK `registerApp` 创建用户独立应用，结果立即加密落库，并自动配置/发布应用。
5. 用户授权入口：`src/app/api/feishu/integrations/[integrationId]/authorize/start/route.ts` 生成标准 OAuth 地址；`src/app/api/feishu/oauth/callback/route.ts` 换取并加密保存真实 Token。
6. 初始化编排：应用校验、用户 Token、组织、当前用户对目标 Base/Table 的可访问性和用户权限依次通过后，先持久化用户级妙记订阅状态，再允许启动事件长连接；六段门禁状态均写入数据库，不维护组织级字段模板检查状态。
7. 事件校验入口：`src/app/api/feishu/integrations/[integrationId]/event-subscription/check/route.ts` 启动 SDK `WSClient` 并等待 ready，ready 是完成烟花的唯一触发状态。
8. `analysisService.ts` 统一调用豆包 API，返回对齐 `Project 2` 的结构化分析结果，并在服务端执行 JSON 修复、Zone 决策树修正、analysis 重写与发言占比重算。
9. `webAnalysisTaskStore.ts` / `webAnalysisTaskProcessor.ts` 负责网页上传任务的落库、领取、执行、完成/失败状态更新。
10. `eventListenerManager.ts` 负责按集成建立 SDK 长连接、消费妙记生成事件，并把事件写入数据库任务表；未满足前置门禁的集成不会启动。
11. `meetingPipelineTaskStore.ts` 负责承接会议任务的落库、领取、重试计划、完成/失败状态与启动恢复读取。
12. `meetingPipelineWorker.ts` 作为常驻任务执行器，轮询数据库领取到期任务，再执行 `minute_token -> transcript -> analysis -> Base 回写` 主链路。
13. `src/instrumentation.ts` 在服务启动时先拉起任务执行器，再基于数据库任务表执行启动恢复。
14. `src/lib/feishu/monitor.ts` 统一输出飞书录制文件、导出、分析和恢复的结构化日志。
15. 报告页 `src/app/report/page.tsx` 通过 `recordId` 从 Base 读取 `JSON数据` 渲染报告；报告模板由 `AnalysisDashboard.tsx` 承载，现分为“团队状态 / 互动网络 / 未完形与非共识 / 领导建议”四部分。

## 依赖说明

| 包 | 用途 |
|-----|------|
| `next` | Next.js 框架 |
| `react` | React 库 |
| `typescript` | TypeScript 编译器 |
| `mammoth` | Word 文档（.docx）解析 |
| `html-to-image` | DOM 转图片，用于 PDF 导出 |
| `jspdf` | PDF 生成 |
| `recharts` | 图表绘制 |
| `drizzle-orm` / `drizzle-kit` | 数据模型、SQL 生成与推送 |
| `pg` | PostgreSQL / Supabase 数据库连接 |
| `@supabase/supabase-js` / `@supabase/ssr` | Supabase Auth 与服务端会话管理 |
| `@larksuiteoapi/node-sdk` | 一键创建应用、OAuth/OpenAPI 与事件长连接 |

## 相关文档

- [后台分析逻辑说明.md](./后台分析逻辑说明.md)
- [输出报告的内容与形式说明.md](./输出报告的内容与形式说明.md)
- [飞书集成设计.md](./飞书集成设计.md)
- [Project2会议分析链路迁移方案.md](./Project2会议分析链路迁移方案.md)
- [报告持久化改造计划.md](./报告持久化改造计划.md)
- [系统架构图.md](./系统架构图.md)
- [AGENTS.md](../AGENTS.md)
