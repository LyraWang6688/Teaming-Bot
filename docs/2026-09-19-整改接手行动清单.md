# Teaming-Bot 整改接手行动清单（代码 + 数据库两维度）

> 日期：2026-09-19
> 基线代码：本地 `main` = `origin/main` @ `424eed1`，另有 **22 个已跟踪文件未提交改动 + 5 个未跟踪文件**
> 线上库：Supabase `titwuhsuaxsglqonksfw`（Postgres 17.6），结构结论来自当日**只读**对账 + 一次**事务内预演并回滚（零落地）**
> 依据：`Teaming-Bot-双维度改造执行规格与验收标准.md`、`2026-09-19-Teaming-Bot-首轮验收报告.md`（F01–F08、A01–A08、B01–B16）

---

## 维度一：数据库迁移

### 1.1 线上现状（已实测）

- 迁移登记表 `supabase_migrations.schema_migrations` 最新只到 `20260722122819`；之后迁移全部手工执行、未登记；`drizzle.__drizzle_migrations` 不存在。
- 已手工建好（无需重建）：
  - 表：`feishu_setup_attempts`、`meeting_base_sync_tasks`、`meeting_report_notification_tasks`、`base_field_bindings`
  - 列：`meeting_records.{data_version,report_revision,recipient_open_id,recipient_app_id,recipient_source,recipient_verified_at}`；`feishu_integrations.{first_initialized_at,first_initialized_evidence}`；`base_field_bindings.{bound_by,bound_at}`
  - 两张任务表 / setup_attempts 的唯一索引、租约索引、外键（ON DELETE CASCADE）、7 态 status CHECK
  - 3 个 ops 视图，`security_invoker=on`，仅 `service_role` 有 SELECT
  - 5 张表 RLS 启用、0 policy（默认拒绝普通角色，方向正确）
- 数据（DISTINCT 去重，未被会议 JOIN 膨胀）：
  - 全局未删除集成 **26** 条：仅 **2** 条有 `first_initialized_at`、**9** 条有旧 `initialized_at`
  - 第三期 `43e21031`（202607）：**24** 条集成，新字段全空，**7** 条有旧字段，**199** 场会议
  - **第四期 `c70342a7`（202609）：2 条集成，`first_initialized_at` 2/2 齐全，2 个用户，6 场会议**，且 2 条均正确关联本期组织方向
  - 4 张新表均 0 行；`meeting_records` 共 211 行，历史 `data_version` 全为 0

### 1.2 范围决策与结构缺口

> **范围决策（2026-09-19，用户确认）：第四期只看新增、不追溯历史。**
> 第四期 2 条集成新字段 2/2 齐全、运维视图 `initialized_integration_total=2` 与实际一致、无漏统。
> 故 F07 的历史回填与"当前可用/新鲜度"口径**本期缓做（D4）**；第三期不纳入即可，视图本就按项目分行。

| # | 缺口 | 影响 | 动作 | 本期 |
|---|---|---|---|---|
| D1 | `feishu_setup_attempts` 缺 `registration_session_hash text` | **F05 上线后点"创建应用"即 500**（线上、迁移、代码三处都缺这列的迁移） | 新迁移 `ADD COLUMN IF NOT EXISTS` + 部分索引 | ✅ 已上线 2026-09-19（列 + 索引，已复核） |
| D2 | `base_field_bindings` 旧 CHECK `..._status_chk(2 态)` 未删，与新 CHECK `..._binding_status_chk(4 态)` 并存，AND 后仍只允许 2 态 | F08 写 `type_mismatch/disabled` 被数据库拒绝 | `DROP CONSTRAINT IF EXISTS ..._status_chk`，保留新 4 态约束 | ✅ 已上线 2026-09-19（仅剩 4 态 CHECK，已复核） |
| D3 | `20260920100000_ops_view_enhancements.sql` 线上整份未执行 | 缺 `ops.ops_delivery_task_details`；项目汇总视图为旧版 | 已幂等补跑 | ✅ 已上线 2026-09-19（明细视图 30 列、汇总视图新列，已复核） |
| D4 | F07 人数口径（历史回填 + 当前可用/新鲜度） | 仅影响第三期历史统计与"配好后又失效"识别 | 重写 ops 视图 + 查询脚本；历史只标"最早可证实时间"，不填造首次时间 | **本期缓做** |
| D5 | 迁移登记断档 | 换环境/他人无法判断线上跑过什么 | 用 `supabase migration repair --status applied` 补登记，此后统一走 CLI | 建议做 |
| D6 | 历史会议 `data_version=0`、无交付任务 | 恢复/重放历史会议的边界 | 维持"不批量补发"；确认恢复路径对 version 0 安全 | 知悉即可 |

### 1.3 可执行 SQL（权威版本，勿在本文档内改写）

- 规范迁移（仓库留档 / 全新环境顺序执行）：`supabase/migrations/20260920120000_setup_session_hash_and_fix_binding_check.sql`（D1+D2）
- **生产手动执行包（用户在 Supabase SQL Editor 逐段运行）**：`supabase/manual_run/2026-09-19_phase4_manual_upgrade.sql`
  - 段 0 执行前预检（只读）；段 1 = D1+D2；段 2 = D3；段 3 执行后验证；段 4 = 可选的迁移登记 CLI
- 该执行包已于 2026-09-19 在生产库**事务内完整预演并 ROLLBACK**：段 1/段 2 全部成功，验证项符合预期，回滚后线上结构恢复原状（列未加、旧约束仍在、新视图未建），确认零落地、可安全手动执行。
- 全新环境迁移顺序：`20260919120000`（建旧 check）→ `20260919130000`（加新 check）→ `20260920120000`（drop 旧 check），最终仅保留 4 态约束。

### 1.4 生产执行与回滚

1. 在 Supabase Dashboard → SQL Editor 新建 query，逐段执行手动包；段 1/段 2 各整段一次 Run（SQL Editor 单次运行在一个事务内，DDL 可回滚；任一句报错则本段不生效，把报错发回）。
2. 部署窗口建议先停/排空交付 worker，避免迁移瞬间写入。
3. 顺序：段 1（D1+D2）→ 段 2（D3）→ 段 3 验证 →（可选）段 4 补登记。
4. 回滚：新增列 nullable、无害可保留；确需回退再 `DROP COLUMN` 并重建旧 2 态约束；视图增强仅新增列/视图、向后兼容，一般无需回退；**不得用删除任务表数据方式回滚**。
5. 执行后把段 3 结果发回核对，并可重跑结构只读对账。

---

## 维度二：未完成的代码修改

### 2.1 F01–F08 完成状态（基于未提交 diff 实测）

| 项 | 级别 | 代码状态 | 说明 / 剩余 |
|---|---|---|---|
| F01 报告与交付登记同事务 | P1 | ✅ 已完成 | 建档/文字稿/报告/失败四阶段均 `getDb().transaction()`；仓储支持传 `tx`；主任务完成入事务 |
| F02 租约清空后重排失败 | P1 | ✅ 已完成 | `completeDeliveryTask` 原子 `case when requested>synced then pending`，带租约+`status='running'` 条件并返回布尔；processor 检查返回值；`syncedVersion` 用实际快照版本 |
| F03 快照固定 Base 目标 | P1 | ✅ 已完成 | 快照加密存 appToken/tableId；worker 从快照解密，删除 `getGlobalBitableConfig`；缺目标 blocked；recordId 只用任务自身的 |
| F04 恢复分阶段同步 | P2 | ✅ 已完成 | 建档=version1、文字稿/报告/失败各 +1 并登记 Base 需求；`greatest()` 只增、succeeded 后才重排 |
| F05 初始化尝试早登记 | P2 | ✅ 代码完成，**D1 已上线** | `beginRegistrationAttempt/resolve/interruptLost` 齐全；执行 D1 后才可用 |
| F06 轮询重置/旧结果覆盖 | P2 | ✅ 已完成 | touch 只增 `state_version` 不动进度时间；resolve 带 token+版本乐观锁；advisory lock + `for update` |
| F07 人数统计口径 | P2 | ⏸️ 本期缓做 | 第四期 2/2 新字段齐全、只看新增，历史回填与"当前可用/新鲜度"均不需要；`markFirstInitializedOnce` 已确认是"仅 firstInitializedAt IS NULL 时写"的正确幂等逻辑 |
| F08 改名修类型丢绑定 | P2 | ✅ 代码完成，**D2 已上线** | reconcile 改为"有 fieldId 即按 ID 找"；执行 D2 删掉旧 CHECK 后 type_mismatch 才能写入 |

### 2.2 剩余代码工作

- [ ] **C1 卫生项**
  - [x] 删除 `src/lib/feishu/integration/integrationSetup.ts` 未使用导入 `toRuntimeErrorContext`（已删除，lint 0 warning）
  - [x] 还原自动生成的 `next-env.d.ts`
  - [ ] 决定 5 个未跟踪文件去留：3 份 docs、`feishu_event_pipeline_flow.html`（用户原图，规格要求保留）、`meeting-category-backfill-preview.csv`
- [ ] **C2 F07 配套代码（本期缓做）**：`markFirstInitializedOnce` 已核对；ops 查询脚本随 F07 缓做，仅"当前交付阻塞清单"可在联调阶段按需补
- [ ] **C3 测试（当前为 0，验收硬门槛）**：引入 Vitest，至少覆盖
  - [ ] B07 事务回滚：报告与两类交付登记同生共死
  - [ ] B08 版本合并：V1 阻塞时 V2/V3 入队，完成后最终到 V3
  - [ ] B12 期次切换：旧任务固定旧目标，不串表
  - [ ] B10/B11 成功回执落库前崩溃 / 超租约晚到结果：unknown 与不盲发
  - [ ] A06 轮询十次不推进 `last_progress_at`、旧回调不覆盖新状态
  - [ ] F08/B13 reconcile：改名→改错类型→恢复类型，fieldId 不丢、不绑到同名字段
  - [ ] 状态机：领取租约、重试退避、blocked 不自动重排
- [ ] **C4 blocked/unknown 恢复**：受控恢复脚本/命令 + 权限 + 审计 + 防成功任务重发说明
- [ ] **C5 质量门**：`pnpm ts-check`、`pnpm lint`、`pnpm build`（构建不得误连生产 worker / 触发真实发送）
- [ ] **C6 文档同步（AGENTS.md 强制）**：`docs/飞书集成设计.md`（阶段事务、目标快照、恢复、迁移、回滚）；新增脚本/文件同步 `docs/项目结构说明.md`
- [ ] **C7 真实联调（B13–B16）**：一场隔离测试会议（测试创建者 + 测试 Base + 测试集成），留存会议 ID/各阶段时间/报告 URL/Base recordId/通知 messageId；无授权则明确标"未验证"

---

## 推荐完成顺序（含验收门）

1. **阶段 0（无争议、不碰生产）**：C1 卫生项；D1+D2 迁移文件（已完成）；`pnpm ts-check/lint` 通过。
2. **阶段 1（测试）**：C3 关键状态机/事务/版本测试；C4 恢复脚本。
3. **阶段 2（隔离验证）**：隔离库执行 D1+D2、补跑 D3 并重跑结构对账；`pnpm build` 通过。
4. **阶段 3（上线）**：用户在 Supabase 执行手动包（D1+D2、D3）+ D5 补登记 → 发回段 3 结果核对 → 部署代码（docker compose）。
5. **阶段 4（联调与交付）**：C7 真实测试会议 → C6 文档 → 输出 F01–F08 / A / B 验收矩阵，未执行项明确标"未验证"。

> F07（D4/C2）不在主路径，作为后续增强；触发条件：第三期重新纳入统计，或第四期出现"配置成功后授权失效/断连"的运营排查诉求。

## 需要用户拍板 / 提供

1. ~~F07"当前可用"新鲜度阈值~~ **已决策：本期不做 F07，无需阈值。**
2. 生产迁移：用户在 Supabase SQL Editor 执行手动包（脚本已提供并预演通过）；执行窗口与是否先停 worker 由用户定。
3. 隔离联调资源：测试飞书群 / 测试集成 / 测试 Base，还是本轮只做到代码 + 数据库级验证。
4. 是否同意引入 Vitest 补 C3 测试；求快可至少补 B07/B08 两个事务与版本合并用例。
5. 字段映射并行工作线的交付状态（`mapping_version` 当前是否有人写入），避免互相覆盖。
6. 5 个未跟踪文件的提交去留。

## 验证命令与只读对账

```bash
pnpm ts-check
pnpm lint
pnpm build
```

结构对账：连接 `DATABASE_URL` 后查询 `information_schema.columns / pg_constraint / pg_indexes / pg_views(reloptions) / pg_policies / role_table_grants / supabase_migrations.schema_migrations`，与本文 1.1/1.2 逐项核对。
